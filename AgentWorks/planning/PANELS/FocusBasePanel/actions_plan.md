# FocusBasePanel Actions Plan

- `LoadFocusTimerAction`: 저장 상태를 불러오고 경과한 타이머를 정리한다.
- `StartFocusTimerAction`: 종료 시각을 계산하고 alarm을 예약한다.
- `PauseFocusTimerAction`: 남은 시간을 저장하고 alarm을 취소한다.
- `ResetFocusTimerAction`: 현재 모드의 기본 시간으로 되돌린다.
- `SaveFocusSettingsAction`: 입력을 검증하고 설정 및 idle timer를 갱신한다.
